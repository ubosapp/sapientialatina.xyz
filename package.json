{
  "name": "verba-latina-app",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend and backend for Verba Latina",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=public/index.js --minify --platform=browser --log-level=warning && cp index.html index.css metadata.json public/"
  },
  "dependencies": {
    "@google/genai": "^1.25.0",
    "lucide-react": "^0.309.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}

{
  "name": "verba-latina-app",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend for Verba Latina",
  "scripts": {
    "build": "mkdir -p public && esbuild index.tsx --bundle --outfile=public/index.js --minify --platform=browser && cp index.html public/index.html && cp index.css public/index.css"
  },
  "dependencies": {
    "@google/genai": "^0.14.0",
    "lucide-react": "^0.309.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}